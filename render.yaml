services:
  - type: web
    name: pdf-toolkit
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /api/health
    envVars:
      - key: NEXT_TELEMETRY_DISABLED
        value: "1"
      - key: PDFTOPPM_PATH
        value: /usr/bin/pdftoppm
